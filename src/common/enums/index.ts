export enum CellType {
  White = "White",
  Black = "Black",
  Blue = "Blue",
}

export enum FigureMoveDirection {
  Horizontal,
  Vertical,
  Diagonal,
}

export enum Event {
  AddFigureToBoard = "AddFigureToBoard",
}
